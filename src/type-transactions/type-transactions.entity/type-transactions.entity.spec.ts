import { TypeTransactionsEntity } from './type-transactions.entity';

describe('TypeTransactionsEntity', () => {
  it('should be defined', () => {
    expect(new TypeTransactionsEntity()).toBeDefined();
  });
});
